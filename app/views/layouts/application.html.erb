<!DOCTYPE html>
<html>
	<head>
		<%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
		<%= javascript_include_tag "application", "data-turbolinks-track" => true %>
		<%= csrf_meta_tags %>
		<title>Муәлік</title>
		<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
	</head>
	<body>

		<nav class="navbar navbar-default navbar-fixed-top">
		
			
			<div class="container">
				<div class="col-lg-push-3 navpills">
					<div class="collapse navbar-collapse">
						<ul class="nav navbar-nav">
						  <li<% if params[:action] == 'index' %> class="active"<% end %>><%= link_to 'Әйгілілері', root_path %></li>
						  <li<% if params[:action] == 'new' %> class="active"<% end %>><%= link_to 'Жаңалары', new_path %></li>
						  <li><a href="#">Өз нұсқамды ұсынайын</a></li>
						</ul>
					</div>
				</div>
			</div>
		
		</nav>

		<div class="container">

			<%= yield %>
			
		</div>

		<footer>
			
		</footer>

	</body>
</html>
